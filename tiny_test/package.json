{
  "name": "eks-fargate-deployment",
  "version": "1.0.0",
  "description": "Deploy an EKS cluster with Fargate using AWS SDK v3",
  "main": "./lib/index.js",
  "type": "module",
  "scripts": {
    "start": "npx ts-node deploy.ts",
    "build": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.500.0",
    "@aws-sdk/client-eks": "^3.500.0",
    "@aws-sdk/client-iam": "^3.500.0",
    "tslint": "^6.1.3"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "license": "MIT",
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts"
}
